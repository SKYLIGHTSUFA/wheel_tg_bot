# Быстрый старт

## 🚀 Рекомендуемый вариант: Деплой на Render.com

Для продакшена рекомендуется использовать **Render.com** - это бесплатный хостинг с постоянным URL и автоматическим HTTPS.

📖 **Подробная инструкция**: см. файл [DEPLOY_RENDER.md](./DEPLOY_RENDER.md)

### Быстрая настройка:

1. Загрузите код в Git репозиторий (GitHub/GitLab/Bitbucket)
2. Зайдите на [render.com](https://render.com) и создайте новый Web Service
3. Подключите репозиторий (Render автоматически определит настройки из `render.yaml`)
4. Установите переменные окружения:
   - `BOT_TOKEN` - токен вашего бота
   - `WEBAPP_URL` - URL вашего приложения (будет доступен после первого деплоя)
5. После деплоя скопируйте URL и добавьте его в `WEBAPP_URL`

Готово! Бот будет работать 24/7 (на бесплатном плане может "засыпать" после 15 минут бездействия).

---

## 🔧 Локальная разработка: Cloudflare Tunnel

Для локальной разработки можно использовать Cloudflare Tunnel.

### 1. Установите cloudflared

### macOS:
```bash
brew install cloudflare/cloudflare/cloudflared
```

### Linux:
```bash
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
chmod +x cloudflared-linux-amd64
sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
```

## 2. Запустите бота и туннель

### Вариант А: Автоматический запуск (рекомендуется)
```bash
./start_with_cloudflare.sh
```

### Вариант Б: Ручной запуск

**Терминал 1 - Запуск бота:**
```bash
python3 bot.py
```

**Терминал 2 - Запуск туннеля:**
```bash
cloudflared tunnel --url http://localhost:8000
```

## 3. Обновите URL в боте

После запуска cloudflared вы получите URL вида:
```
https://random-name.trycloudflare.com
```

Обновите `WEBAPP_URL` в файле `bot.py`:
```python
WEBAPP_URL = "https://random-name.trycloudflare.com"
```

Перезапустите бота.

## 4. Готово! 🎉

Теперь WebApp должен открываться в Telegram на телефоне.

---

---

## Альтернативы

- **Render.com** (рекомендуется) - см. [DEPLOY_RENDER.md](./DEPLOY_RENDER.md)
- **Cloudflare Tunnel** - для локальной разработки (см. выше)
- Другие варианты - см. файл `ALTERNATIVES.md`
