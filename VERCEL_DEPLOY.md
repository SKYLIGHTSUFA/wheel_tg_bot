# Деплой на Vercel

Инструкция по развертыванию Telegram бота на Vercel

## Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
3. Git репозиторий с кодом (GitHub, GitLab или Bitbucket)
4. Установленный [Vercel CLI](https://vercel.com/docs/cli) (опционально, для локального тестирования)

## Структура проекта

Проект должен содержать следующие файлы:
- `bot.py` - основной файл приложения с FastAPI
- `api/index.py` - serverless function для обработки API запросов
- `api/webhook.py` - serverless function для обработки webhook от Telegram
- `vercel.json` - конфигурация для Vercel
- `requirements.txt` - зависимости Python
- `index.html` - WebApp интерфейс

## Шаги деплоя

### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены в Git репозиторий:
```bash
git add .
git commit -m "Подготовка для деплоя на Vercel"
git push
```

### 2. Создание проекта на Vercel

#### Вариант A: Через веб-интерфейс

1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите "Add New..." → "Project"
3. Импортируйте ваш Git репозиторий
4. Vercel автоматически определит настройки из `vercel.json`

#### Вариант B: Через CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой проекта
vercel

# Для продакшена
vercel --prod
```

### 3. Настройка переменных окружения

В настройках проекта на Vercel (Settings → Environment Variables) добавьте:

**Обязательные:**
- `BOT_TOKEN` - токен вашего Telegram бота

**Опциональные (если нужно изменить значения по умолчанию):**
- `WEBAPP_URL` - URL вашего проекта на Vercel (устанавливается автоматически после деплоя)
- `SHOP_ADDRESS` - адрес магазина
- `SHOP_PHONE` - телефон магазина
- `DB_PATH` - путь к базе данных (по умолчанию `/tmp/db.sqlite3` на Vercel)

### 4. Настройка WebApp URL

После первого деплоя:

1. Скопируйте URL вашего проекта из Vercel Dashboard (например: `https://wheel-tg-bot.vercel.app`)
2. В настройках проекта добавьте переменную окружения `WEBAPP_URL` со значением вашего URL
3. Перезапустите деплой (Redeploy) или подождите автоматического перезапуска

### 5. Настройка Webhook для Telegram
  
После деплоя нужно настроить webhook для бота:  

#### Способ 1: Через API endpoint

После деплоя откройте в браузере:
```
https://your-project.vercel.app/api/set-webhook
```

Или используйте curl:
```bash
curl -X POST "https://your-project.vercel.app/api/set-webhook"
```

#### Способ 2: Вручную через Telegram Bot API

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-project.vercel.app/api/webhook"}'
```

Замените:
- `<YOUR_BOT_TOKEN>` на ваш токен бота
- `your-project.vercel.app` на URL вашего проекта

#### Проверка webhook

Проверить статус webhook можно через:
```
https://your-project.vercel.app/api/webhook-info
```

### 6. Проверка работы

1. Откройте ваш бот в Telegram
2. Отправьте команду `/start`
3. Проверьте, что кнопка "🛞 Открыть магазин" работает
4. Протестируйте создание заказа

## Важные замечания

### База данных SQLite на Vercel

⚠️ **Внимание**: На Vercel используется serverless архитектура с ограничениями:

1. **Файловая система read-only**: Используется `/tmp` для временных файлов
2. **Данные не сохраняются**: При каждом вызове функции создается новая среда
3. **Рекомендация**: Для продакшена используйте внешнюю БД:
   - PostgreSQL (через Vercel Postgres или внешний сервис)
   - MongoDB Atlas
   - Supabase
   - Другие облачные БД

### Serverless функции

- Каждый запрос обрабатывается в отдельной функции
- Холодный старт может занимать несколько секунд
- Для лучшей производительности используйте платный план Vercel

### Webhook vs Polling

На Vercel используется **только webhook**, так как:
- Serverless функции не поддерживают долгие процессы (polling)
- Webhook более эффективен для serverless архитектуры
- Меньше задержек в обработке сообщений

### Ограничения бесплатного плана

На бесплатном плане Vercel:
- 100 GB-hours выполнения функций в месяц
- 1000 invocations в день для serverless functions
- Для продакшена рекомендуется использовать платный план

## Обновление приложения

После внесения изменений в код:

1. Закоммитьте изменения в Git
2. Запушьте в репозиторий
3. Vercel автоматически пересоберет и задеплоит проект

Или используйте CLI:
```bash
vercel --prod
```

## Мониторинг

В Vercel Dashboard вы можете:
- Просматривать логи функций (Functions → Logs)
- Мониторить использование ресурсов (Analytics)
- Настраивать автоматические деплои из Git (Deployments)

## Устранение проблем

### Бот не отвечает

1. Проверьте, что webhook установлен: `https://your-project.vercel.app/api/webhook-info`
2. Проверьте логи в Vercel Dashboard
3. Убедитесь, что `BOT_TOKEN` установлен правильно

### Ошибки с базой данных

1. Убедитесь, что используется `/tmp` для БД (автоматически на Vercel)
2. Рассмотрите переход на внешнюю БД для продакшена

### WebApp не открывается

1. Проверьте, что `WEBAPP_URL` установлен правильно
2. Убедитесь, что URL не содержит слеш в конце
3. Проверьте логи в Vercel Dashboard

## Миграция с Render.com

Если вы мигрируете с Render.com:

1. Удалите файлы `render.yaml` и `RENDER_DEPLOY.md` (опционально)
2. Убедитесь, что все переменные окружения перенесены в Vercel
3. Настройте webhook вместо polling
4. Обновите `WEBAPP_URL` на новый URL Vercel

## Поддержка

При возникновении проблем:
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `BOT_TOKEN` корректный
4. Убедитесь, что webhook настроен правильно

## Дополнительные ресурсы

- [Документация Vercel](https://vercel.com/docs)
- [Vercel Python Runtime](https://vercel.com/docs/concepts/functions/serverless-functions/runtimes/python)
- [Telegram Bot API](https://core.telegram.org/bots/api)
