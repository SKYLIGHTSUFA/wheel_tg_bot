# Деплой на Render.com

Инструкция по развертыванию Telegram бота на Render.com

## Предварительные требования

1. Аккаунт на [Render.com](https://render.com)
2. Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
3. Git репозиторий с кодом (GitHub, GitLab или Bitbucket)

## Шаги деплоя

### 1. Подготовка репозитория

Убедитесь, что в репозитории есть следующие файлы:
- `bot.py` - основной файл приложения
- `requirements.txt` - зависимости Python
- `render.yaml` - конфигурация для Render.com
- `index.html` - WebApp интерфейс

### 2. Создание сервиса на Render.com

1. Войдите в [Render Dashboard](https://dashboard.render.com)
2. Нажмите "New +" → "Web Service"
3. Подключите ваш Git репозиторий
4. Render автоматически определит настройки из `render.yaml`

### 3. Настройка переменных окружения

В настройках сервиса (Environment) добавьте следующие переменные:

**Обязательные:**
- `BOT_TOKEN` - токен вашего Telegram бота

**Опциональные (если нужно изменить значения по умолчанию):**
- `WEBAPP_URL` - URL вашего сервиса на Render.com (обычно устанавливается автоматически)
- `SHOP_ADDRESS` - адрес магазина
- `SHOP_PHONE` - телефон магазина
- `DB_PATH` - путь к базе данных (по умолчанию `db.sqlite3`)

### 4. Настройка WebApp URL

После деплоя:
1. Дождитесь успешного деплоя сервиса
2. Скопируйте URL вашего сервиса из Render Dashboard (например: `https://wheel-tg-bot.onrender.com`)
3. В настройках сервиса (Environment) добавьте или обновите переменную `WEBAPP_URL` со значением вашего URL
4. Сохраните изменения - Render автоматически перезапустит сервис
5. Убедитесь, что URL заканчивается без слеша (например: `https://wheel-tg-bot.onrender.com`, а не `https://wheel-tg-bot.onrender.com/`)

### 5. Проверка работы

1. Откройте ваш бот в Telegram
2. Отправьте команду `/start`
3. Проверьте, что кнопка "🛞 Открыть магазин" работает
4. Протестируйте создание заказа

## Важные замечания

### База данных SQLite

⚠️ **Внимание**: SQLite на Render.com имеет ограничения:
- Данные могут быть потеряны при перезапуске сервиса (на бесплатном плане)
- Для продакшена рекомендуется использовать PostgreSQL

Для использования PostgreSQL:
1. Создайте PostgreSQL сервис на Render.com
2. Обновите код для работы с PostgreSQL вместо SQLite
3. Добавьте переменную окружения `DATABASE_URL`

### Бесплатный план Render.com

На бесплатном плане:
- Сервис "засыпает" после 15 минут неактивности
- Первый запрос после "сна" может занять до 30 секунд
- Для постоянной работы рекомендуется использовать платный план

### Webhook vs Polling

Текущая версия использует polling. Для лучшей производительности можно настроить webhook:
1. Получите URL вашего сервиса
2. Настройте webhook через Telegram Bot API
3. Обновите код для работы с webhook вместо polling

## Обновление приложения

После внесения изменений в код:
1. Закоммитьте изменения в Git
2. Запушьте в репозиторий
3. Render автоматически пересоберет и перезапустит сервис

## Мониторинг

В Render Dashboard вы можете:
- Просматривать логи приложения
- Мониторить использование ресурсов
- Настраивать автоматические деплои из Git

## Поддержка

При возникновении проблем:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `BOT_TOKEN` корректный
