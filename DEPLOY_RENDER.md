# Деплой на Render.com

Это руководство поможет вам развернуть Telegram бота на Render.com.

## Преимущества Render.com

- ✅ Бесплатный план доступен
- ✅ Автоматический HTTPS
- ✅ Постоянный URL (не меняется при перезапуске)
- ✅ Автоматический деплой из Git
- ✅ Простая настройка переменных окружения

## Шаг 1: Подготовка репозитория

Убедитесь, что ваш код загружен в Git репозиторий (GitHub, GitLab или Bitbucket).

## Шаг 2: Создание сервиса на Render.com

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь/войдите
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш Git репозиторий
4. Render автоматически определит настройки из `render.yaml`

### Или настройте вручную:

- **Name**: `wheel-tg-bot` (или любое другое имя)
- **Environment**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python bot.py`
- **Plan**: `Free` (или выберите платный план)

## Шаг 3: Настройка переменных окружения

В настройках сервиса (Settings → Environment) добавьте следующие переменные:

### Обязательные:

- **BOT_TOKEN**: Токен вашего Telegram бота (получите у [@BotFather](https://t.me/BotFather))
- **WEBAPP_URL**: URL вашего приложения на Render (будет доступен после первого деплоя)

### Опциональные:

- **DB_PATH**: Путь к базе данных (по умолчанию: `db.sqlite3`)
- **SHOP_ADDRESS**: Адрес магазина (по умолчанию: `г. Уфа, ул. Трамвайная, д. 13/1`)
- **SHOP_PHONE**: Телефон магазина (по умолчанию: `+79177364777`)

## Шаг 4: Получение URL приложения

После первого деплоя Render предоставит вам URL вида:
```
https://wheel-tg-bot.onrender.com
```

**Важно**: Скопируйте этот URL и добавьте его в переменную окружения **WEBAPP_URL** в настройках сервиса.

## Шаг 5: Перезапуск сервиса

После добавления `WEBAPP_URL` перезапустите сервис:
1. Перейдите в **Manual Deploy** → **Deploy latest commit**
2. Или просто подождите автоматического деплоя

## Шаг 6: Проверка работы

1. Откройте ваш бот в Telegram
2. Отправьте команду `/start`
3. Должна появиться кнопка "🛞 Открыть магазин"
4. Нажмите на неё - должен открыться WebApp

## Важные замечания

### Бесплатный план Render.com

- ⚠️ Приложение "засыпает" после 15 минут бездействия
- ⚠️ Первый запрос после пробуждения может занять 30-60 секунд
- ⚠️ Для продакшена рекомендуется использовать платный план

### База данных

- SQLite файл хранится в файловой системе приложения
- При перезапуске/передеплое данные сохраняются
- Для более надежного хранения рассмотрите использование PostgreSQL (Render предоставляет бесплатный план)

### Обновление кода

Render автоматически деплоит изменения при push в основную ветку (main/master).
Вы также можете настроить автодеплой из других веток в настройках.

## Устранение проблем

### Бот не отвечает

1. Проверьте логи в панели Render (Logs)
2. Убедитесь, что `BOT_TOKEN` установлен правильно
3. Проверьте, что бот запущен (Status должен быть "Live")

### WebApp не открывается

1. Убедитесь, что `WEBAPP_URL` установлен и указывает на правильный URL
2. URL должен начинаться с `https://`
3. Проверьте, что приложение доступно по этому URL в браузере

### Ошибки при деплое

1. Проверьте логи сборки (Build Logs)
2. Убедитесь, что все зависимости указаны в `requirements.txt`
3. Проверьте, что `render.yaml` имеет правильный синтаксис

## Миграция с Cloudflare Tunnel

Если вы ранее использовали Cloudflare Tunnel:

1. Удалите файлы `start_with_cloudflare.sh` и `setup_cloudflare_tunnel.md` (опционально)
2. Обновите `WEBAPP_URL` в настройках Render на новый URL
3. Перезапустите сервис

## Дополнительные ресурсы

- [Документация Render.com](https://render.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram WebApp](https://core.telegram.org/bots/webapps)
